{% extends 'base.html.twig' %}

{% block title %}Hello ApiController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}! ✅</h1>

        <h1>BEERS</h1>
        <ul>
            {% for beer in data %}
                <fieldset>
                    <li>{{ beer.id }}</li>
                    <li>{{ beer.name }}</li>
                    <li>{{ beer.description }}<br>
                      {% for food in beer.food %}
                        {{ food }}<br>
                    </li>
                    {% endfor %}
                </fieldset>


            {% endfor %}
        </ul>

        <ul>
            <li>Your controller at <code><a
                            href="{{ '/home/pablo/code/api/src/Controller/ApiController.php'|file_link(0) }}">src/Controller/ApiController.php</a></code>
            </li>
            <li>Your template at <code><a
                            href="{{ '/home/pablo/code/api/templates/api/index.html.twig'|file_link(0) }}">templates/api/index.html.twig</a></code>
            </li>
        </ul>
    </div>
{% endblock %}
